import { CheckcountPipe } from './checkcount.pipe';

describe('CheckcountPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckcountPipe();
    expect(pipe).toBeTruthy();
  });
});
